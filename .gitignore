# python
__pycache__/
*.pyc
.venv/
.ruff_cache/
.pytest_cache/

# node / observable framework
node_modules/
site/dist/
.observablehq/

# NOTE: site/src/data/*.json (the published aggregate snapshot) IS committed, so
# the GitHub Pages build can run without access to the private lake. Refresh it
# with `make extract` and commit the result whenever the lake is updated.

# Working files from the code-mapping workflow. institution-codes.csv IS committed
# (it is curated input); these are its throwaway inputs — candidate rows straight from
# the registry, and `podlake-web probe` output.
*-candidates.csv
codes.csv

# local lake / env
*.ducklake
.env
